# Python packaging only. Desktop UI uses system PyGObject/GTK.
#
# Fedora / Nobara:
#   sudo dnf install python3-gobject gtk4 libadwaita
# Optional video backend:
#   sudo dnf install mpv
#
# Install this project in editable mode:
#   pip install -e .
